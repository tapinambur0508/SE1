let status: "success" | "loading" | "error" = "success"; // "loading", "error"

status = "loading";
status = "unknown";

let flag: true | false = false;
let code: 200 | 201 | 400 | 404 | 500 = 200;

code = 1000;

type Theme = "light" | "black";

let theme: Theme = "light";
theme = "yellow";
